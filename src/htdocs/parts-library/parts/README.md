# parts

単体で使えるコンポーネントを置きます。

## 追加候補

- `accordion/`: FAQや開閉コンテンツ
- `modal/`: お知らせ、画像拡大、動画表示
- `tab/`: タブ切り替え
- `slider/`: カルーセル、スライダー
- `form/`: 入力欄、エラー表示、確認画面まわり
- `pagination/`: ページャー
- `sns-share/`: SNSシェアリンク

各パーツは、EJS / SCSS / JSをまとめて保管します。JSが不要なパーツは `.js` を置かなくてもかまいません。

